const app = Vue.createApp({
    data() {
        return {
            product: 'Boots',
            description: 'This is a product description with a description key within our data object.'
        }
    }
})
